apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: prs-typesense
spec:
  template:
    spec:
      containers:
        - name: prs-typesense
          envFrom:
            - secretRef:
                name: prs-env-prod
          startupProbe:
            httpGet:
              path: /health/
              port: 8108
              scheme: HTTP
            # Allow sufficient time for prod collections to be loaded.
            initialDelaySeconds: 300
            periodSeconds: 15
            timeoutSeconds: 3
            successThreshold: 1
            failureThreshold: 3
          resources:
            # Prod workload requires a larger request & limit for memory.
            requests:
              memory: '2Gi'
            limits:
              memory: '6Gi'
          volumeMounts:
            - mountPath: /data
              name: prs-typesense-data
      volumes:
        - name: prs-typesense-data
          persistentVolumeClaim:
            claimName: prs-typesense-data-prod
